import React, {useState, useEffect} from "react";

/*function useFetchLogin(userItem){
    const [userItem, setData] = useState([])
}
useEffect(()=> {
    if(userItem[0]!="" && userItem[1]!="")
    setData([...userItem,userItem[0]])
});
return userItem*/
